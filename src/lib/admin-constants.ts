/** Cookie-Name für die Admin-Session (Middleware + Server). */
export const ADMIN_SESSION_COOKIE = "rezepte_admin_session";

/** Standard-PIN nach Ersteinrichtung (in DB überschreiben). */
export const DEFAULT_ADMIN_PIN = "0000";
